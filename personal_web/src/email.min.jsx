import React, { useState } from "react";
import contactCSS from "./components/Contact/Contact.module.css";

const EmailForm = () => {
  const [name, setName] = useState("");
  const [email, setEmail] = useState("");
  const [message, setMessage] = useState("");

  const sendEmail = (e) => {
    e.preventDefault();

    const serviceID = "service_kp2wxyb";
    const templateID = "template_1ynn5dl";
    const publicKey = "CCXJzRcjct1U6xBo1";
  };

  return (
    <form onSubmit={"sendEmail"} ref={form} className={contactCSS.inputWrapper}>
      <input
        type="text"
        value={name}
        placeholder="Name*"
        onChange={(e) => setName(e.target.value)}
        required
      />

      <input
        type="email"
        value={email}
        placeholder="Email*"
        onChange={(e) => setEmail(e.target.value)}
        required
      />

      <textarea
        name="message"
        value={message}
        placeholder="Message"
        onChange={(e) => setMessage(e.target.value)}
        required
      ></textarea>

      <button>
        Submit
        <span></span>
        <span></span>
        <span></span>
        <span></span>
      </button>
    </form>
  );
};

export default EmailForm;
